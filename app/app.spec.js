/* global main, describe, it, expect, should */

describe('main()', function () {
  'use strict';

  it('exists', function () {
    expect(main).to.be.a('function');

  });

  it('does something', function () {
    expect(true).to.equal(false);
  });

  it('does something else', function () {
    expect(true).to.equal(false);
  });

  // Add more assertions here
});
